[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "easylattice-local-runner"
version = "0.1.0"
description = "easyLattice localhost companion runner"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["app"]
